[toolchain]
# Pin release and CI builds to MSRV. This prevents compiler drift from changing
# generated code or silently widening the accepted toolchain surface.
channel = "1.89.0"
components = ["rustfmt", "clippy"]
# GPU/edge targets added per-backend; wasm + aarch64 land with those crates.
