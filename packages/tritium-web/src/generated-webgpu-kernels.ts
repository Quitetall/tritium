// @generated by scripts/generate-webgpu-kernels.mjs; do not edit.
export const WEBGPU_KERNEL_BUNDLE_SHA256_V1 = "7390c86038d8f31da728aaaa1727f0b918a8a6b12787d3ba0d769d802d058693" as const;

export const WEBGPU_DISPATCH_CATALOG_SHA256_V1 = "214f78c03c2a3bee0bfff4d2a2427d0ed72eedf25cd0f47aa7f20a32742ab86a" as const;

export const WEBGPU_KERNEL_MODULES_V1 = Object.freeze({
  "adamw": Object.freeze({
    id: "adamw",
    sha256: "56e3aa6e9c9acbd4d90d24bd0aade67b57fdbe3b9e8729b59c4e3554bcc60628",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> parameter: array<f32>;\n@group(0) @binding(2) var<storage, read> gradient: array<f32>;\n@group(0) @binding(3) var<storage, read> moment1: array<f32>;\n@group(0) @binding(4) var<storage, read> moment2: array<f32>;\n@group(0) @binding(5) var<storage, read_write> updated_parameter: array<f32>;\n@group(0) @binding(6) var<storage, read_write> updated_moment1: array<f32>;\n@group(0) @binding(7) var<storage, read_write> updated_moment2: array<f32>;\n@group(0) @binding(8) var<storage, read_write> scratch: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let g = gradient[index];\n        updated_moment1[index] = params.beta1 * moment1[index];\n        scratch[index] = (1.0 - params.beta1) * g;\n        updated_moment2[index] = params.beta2 * moment2[index];\n        scratch[params.len + index] = (1.0 - params.beta2) * g;\n        updated_parameter[index] = parameter[index] * params.shrink;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "adamw_finish": Object.freeze({
    id: "adamw_finish",
    sha256: "b8c068132971f440e8fa6e2f54cac61e4f363b4b9ba1c963f15e80d7e7b0b090",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(5) var<storage, read_write> updated_parameter: array<f32>;\n@group(0) @binding(6) var<storage, read_write> updated_moment1: array<f32>;\n@group(0) @binding(7) var<storage, read_write> updated_moment2: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let m = updated_moment1[index];\n        let v = updated_moment2[index];\n        let adaptive = params.learning_rate\n            * (m / params.correction1 / (sqrt(v / params.correction2) + params.epsilon));\n        updated_parameter[index] = updated_parameter[index] - adaptive;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "adamw_terms": Object.freeze({
    id: "adamw_terms",
    sha256: "82fc47ae01a9e68df40c0430dc465e1a7bf3dc71f85f25178ed530b2e492c0e4",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(6) var<storage, read_write> updated_moment1: array<f32>;\n@group(0) @binding(8) var<storage, read_write> scratch: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        updated_moment1[index] = updated_moment1[index] + scratch[index];\n        scratch[params.len + index] = scratch[params.len + index] * gradient_value(index);\n    }\n}\n\nfn gradient_value(index: u32) -> f32 {\n    return gradient[index];\n}\n\n@group(0) @binding(2) var<storage, read> gradient: array<f32>;\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "adamw_variance": Object.freeze({
    id: "adamw_variance",
    sha256: "7268447e471e94fe2e7adda185ac27c618c8e90ef9ae9bde2cd5d738d394e119",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(7) var<storage, read_write> updated_moment2: array<f32>;\n@group(0) @binding(8) var<storage, read_write> scratch: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        updated_moment2[index] = updated_moment2[index] + scratch[params.len + index];\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "attention": Object.freeze({
    id: "attention",
    sha256: "6f7a4e028cb8d3dbaf1161f47d5714f26cf181d431f734fc04f46c32f7180092",
    source: "struct AttentionParams {\n    seq: u32,\n    n_head: u32,\n    n_kv_head: u32,\n    head_dim: u32,\n    causal: u32,\n    execution: u32,\n    padding_0: u32,\n    padding_1: u32,\n}\n\n// Chrome Tint rejects a bitcast infinity literal. This finite sentinel still\n// underflows through exp() and preserves masked-softmax behavior.\nconst NEGATIVE_LARGE: f32 = -3.402823466e+38;\n\n@group(0) @binding(0) var<uniform> params: AttentionParams;\n@group(0) @binding(1) var<storage, read> q: array<f32>;\n@group(0) @binding(2) var<storage, read> k: array<f32>;\n@group(0) @binding(3) var<storage, read> v: array<f32>;\n@group(0) @binding(4) var<storage, read> grad_output: array<f32>;\n@group(0) @binding(5) var<storage, read_write> output_0: array<f32>;\n@group(0) @binding(6) var<storage, read_write> output_1: array<f32>;\n@group(0) @binding(7) var<storage, read_write> output_2: array<f32>;\n@group(0) @binding(8) var<storage, read_write> probabilities: array<f32>;\n@group(0) @binding(9) var<storage, read_write> grad_probabilities: array<f32>;\n\nfn vector_index(token: u32, head: u32, head_count: u32, lane: u32) -> u32 {\n    return (token * head_count + head) * params.head_dim + lane;\n}\n\nfn compute_probabilities(head: u32, kv_head: u32, scale: f32) {\n    for (var query = 0u; query < params.seq; query++) {\n        let row = query * params.seq;\n        for (var key = 0u; key < params.seq; key++) {\n            var score = 0.0;\n            for (var lane = 0u; lane < params.head_dim; lane++) {\n                score += q[vector_index(query, head, params.n_head, lane)]\n                    * k[vector_index(key, kv_head, params.n_kv_head, lane)];\n            }\n            probabilities[row + key] = select(score * scale, NEGATIVE_LARGE, params.causal != 0u && key > query);\n        }\n        var maximum = NEGATIVE_LARGE;\n        for (var key = 0u; key < params.seq; key++) {\n            maximum = max(maximum, probabilities[row + key]);\n        }\n        var sum = 0.0;\n        for (var key = 0u; key < params.seq; key++) {\n            let exponential = select(exp(probabilities[row + key] - maximum), 0.0, params.causal != 0u && key > query);\n            probabilities[row + key] = exponential;\n            sum += exponential;\n        }\n        for (var key = 0u; key < params.seq; key++) {\n            probabilities[row + key] /= sum;\n        }\n    }\n}\n\n@compute @workgroup_size(1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    if (gid.x != 0u || gid.y != 0u || gid.z != 0u) {\n        return;\n    }\n    let group_size = params.n_head / params.n_kv_head;\n    let scale = inverseSqrt(f32(params.head_dim));\n    if (params.execution == 0u) {\n        for (var head = 0u; head < params.n_head; head++) {\n            let kv_head = head / group_size;\n            compute_probabilities(head, kv_head, scale);\n            for (var query = 0u; query < params.seq; query++) {\n                for (var lane = 0u; lane < params.head_dim; lane++) {\n                    var accumulator = 0.0;\n                    for (var key = 0u; key < params.seq; key++) {\n                        accumulator += probabilities[query * params.seq + key]\n                            * v[vector_index(key, kv_head, params.n_kv_head, lane)];\n                    }\n                    output_0[vector_index(query, head, params.n_head, lane)] = accumulator;\n                }\n            }\n        }\n        return;\n    }\n\n    // Output buffers are newly allocated storage resources and have no\n    // defined initial contents. VJP writes accumulate across query/head loops,\n    // so clear every destination before the first accumulation.\n    for (var index = 0u; index < params.seq * params.n_head * params.head_dim; index++) {\n        output_0[index] = 0.0;\n    }\n    for (var index = 0u; index < params.seq * params.n_kv_head * params.head_dim; index++) {\n        output_1[index] = 0.0;\n        output_2[index] = 0.0;\n    }\n\n    var head_cursor = params.n_head;\n    loop {\n        if (head_cursor == 0u) {\n            break;\n        }\n        head_cursor -= 1u;\n        let head = head_cursor;\n        let kv_head = head / group_size;\n        compute_probabilities(head, kv_head, scale);\n        for (var index = 0u; index < params.seq * params.seq; index++) {\n            grad_probabilities[index] = 0.0;\n        }\n        for (var query = 0u; query < params.seq; query++) {\n            for (var lane = 0u; lane < params.head_dim; lane++) {\n                let gradient = grad_output[vector_index(query, head, params.n_head, lane)];\n                for (var key = 0u; key < params.seq; key++) {\n                    let probability_index = query * params.seq + key;\n                    let value_index = vector_index(key, kv_head, params.n_kv_head, lane);\n                    grad_probabilities[probability_index] += gradient * v[value_index];\n                    output_2[value_index] += gradient * probabilities[probability_index];\n                }\n            }\n        }\n        for (var query = 0u; query < params.seq; query++) {\n            let row = query * params.seq;\n            var contraction = 0.0;\n            for (var key = 0u; key < params.seq; key++) {\n                contraction += probabilities[row + key] * grad_probabilities[row + key];\n            }\n            for (var key = 0u; key < params.seq; key++) {\n                let index = row + key;\n                grad_probabilities[index] = select(\n                    probabilities[index]\n                        * (grad_probabilities[index] - contraction) * scale,\n                    0.0,\n                    params.causal != 0u && key > query,\n                );\n            }\n        }\n        for (var query = 0u; query < params.seq; query++) {\n            for (var key = 0u; key < params.seq; key++) {\n                let gradient = grad_probabilities[query * params.seq + key];\n                for (var lane = 0u; lane < params.head_dim; lane++) {\n                    let query_index = vector_index(query, head, params.n_head, lane);\n                    let key_index = vector_index(key, kv_head, params.n_kv_head, lane);\n                    output_0[query_index] += gradient * k[key_index];\n                    output_1[key_index] += gradient * q[query_index];\n                }\n            }\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":9,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":9,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([1,1,1]) as readonly [number, number, number]}),
  }),
  "byte_codec": Object.freeze({
    id: "byte_codec",
    sha256: "b5e47336a0a5a90b707259d4e9fad097a3f6c8d5661c0f4b29a8521ce72045d7",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read_write> source: array<u32>;\n@group(0) @binding(2) var<storage, read_write> destination: array<u32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn unpack(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let word = source[index / 4u];\n        destination[index] = (word >> ((index % 4u) * 8u)) & 255u;\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn pack(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let word_index = gid.x;\n    let words = params.len / 4u + select(0u, 1u, params.len % 4u != 0u);\n    if (word_index < words) {\n        let first = word_index * 4u;\n        var word = 0u;\n        for (var lane = 0u; lane < 4u; lane += 1u) {\n            let index = first + lane;\n            if (index < params.len) {\n                word |= (source[index] & 255u) << (lane * 8u);\n            }\n        }\n        destination[word_index] = word;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"unpack": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read_write"})]),"pack": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"unpack": Object.freeze([64,1,1]) as readonly [number, number, number],"pack": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "cautious_adamw_finish": Object.freeze({
    id: "cautious_adamw_finish",
    sha256: "08c0443dea74b61b85ee14c3cede23d6cf4ef376595ffebd429b6bf133a8efbb",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(5) var<storage, read_write> updated_parameter: array<f32>;\n@group(0) @binding(8) var<storage, read_write> scratch1: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        updated_parameter[index] = updated_parameter[index] - scratch1[index];\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "cautious_adamw_lr": Object.freeze({
    id: "cautious_adamw_lr",
    sha256: "658400facb30d17a4400e2dda6cf4ef94d682df40fd64962af331d59e217ea95",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(8) var<storage, read_write> scratch1: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        scratch1[index] = params.learning_rate * scratch1[index];\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "cautious_adamw_mask": Object.freeze({
    id: "cautious_adamw_mask",
    sha256: "6fb6a5b41acc57e5472ed02cff12e7980a151476cf5ac3df9d88313a5d3bc787",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(2) var<storage, read> gradient: array<f32>;\n@group(0) @binding(6) var<storage, read_write> updated_moment1: array<f32>;\n@group(0) @binding(7) var<storage, read_write> updated_moment2: array<f32>;\n@group(0) @binding(8) var<storage, read_write> scratch1: array<f32>;\n@group(0) @binding(10) var<storage, read_write> aligned: atomic<u32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let update = (updated_moment1[index] / params.correction1)\n            / (sqrt(updated_moment2[index] / params.correction2) + params.epsilon);\n        if (update * gradient[index] > 0.0) {\n            scratch1[index] = update;\n            atomicAdd(&aligned, 1u);\n        } else {\n            scratch1[index] = 0.0;\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":10,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":10,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "cautious_adamw_rescale": Object.freeze({
    id: "cautious_adamw_rescale",
    sha256: "555e763b2929c82556131a008c2d43878d6aa1dcb7e26320f7fcc9a70d312c34",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(8) var<storage, read_write> scratch1: array<f32>;\n@group(0) @binding(10) var<storage, read_write> aligned: atomic<u32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let rescale = f32(params.len) / (f32(atomicLoad(&aligned)) + 1.0);\n        scratch1[index] = scratch1[index] * rescale;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":10,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":10,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "concat": Object.freeze({
    id: "concat",
    sha256: "94464a092500777ef46e6cfd0b4022596fce8d2d643beedf6ac1cd8f7b5a8f3e",
    source: "struct Params {\n    rows: u32,\n    part_count: u32,\n    total_columns: u32,\n    padding: u32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> values: array<f32>;\n@group(0) @binding(2) var<storage, read> lengths: array<u32>;\n@group(0) @binding(3) var<storage, read> offsets: array<u32>;\n@group(0) @binding(4) var<storage, read_write> result: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index >= params.rows * params.total_columns) {\n        return;\n    }\n    let row = index / params.total_columns;\n    let output_column = index % params.total_columns;\n    var column_start = 0u;\n    for (var part = 0u; part < params.part_count; part = part + 1u) {\n        let width = lengths[part];\n        if (output_column < column_start + width) {\n            let local_column = output_column - column_start;\n            result[index] = values[offsets[part] + row * width + local_column];\n            return;\n        }\n        column_start = column_start + width;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "conv": Object.freeze({
    id: "conv",
    sha256: "e43ca8d5c73ee0932c14774648e01a05863a2ab7f8d3299b6c67a1b19654f8ad",
    source: "struct ConvParams {\n    batch: u32,\n    c_in: u32,\n    c_out: u32,\n    input_h: u32,\n    input_w: u32,\n    kernel_h: u32,\n    kernel_w: u32,\n    stride_h: u32,\n    stride_w: u32,\n    dilation_h: u32,\n    dilation_w: u32,\n    pad_top: u32,\n    pad_left: u32,\n    groups: u32,\n    output_h: u32,\n    output_w: u32,\n    execution: u32,\n    pad_bottom: u32,\n    pad_right: u32,\n    padding: u32,\n}\n\n@group(0) @binding(0) var<uniform> params: ConvParams;\n@group(0) @binding(1) var<storage, read> x: array<f32>;\n@group(0) @binding(2) var<storage, read> weight: array<f32>;\n@group(0) @binding(3) var<storage, read> scale: array<f32>;\n@group(0) @binding(4) var<storage, read> grad_output: array<f32>;\n@group(0) @binding(5) var<storage, read_write> result: array<f32>;\n@group(0) @binding(6) var<storage, read_write> grad_weight: array<f32>;\n@group(0) @binding(7) var<storage, read_write> grad_scale: array<f32>;\n\nfn input_value(b: u32, channel: u32, oh: u32, ow: u32, kh: u32, kw: u32) -> f32 {\n    let ih = i32(oh * params.stride_h + kh * params.dilation_h) - i32(params.pad_top);\n    let iw = i32(ow * params.stride_w + kw * params.dilation_w) - i32(params.pad_left);\n    if (ih < 0 || ih >= i32(params.input_h) || iw < 0 || iw >= i32(params.input_w)) {\n        return 0.0;\n    }\n    let index = ((b * params.c_in + channel) * params.input_h + u32(ih)) * params.input_w + u32(iw);\n    return x[index];\n}\n\n@compute @workgroup_size(1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    if (gid.x != 0u || gid.y != 0u || gid.z != 0u) {\n        return;\n    }\n    let c_in_per_group = params.c_in / params.groups;\n    let c_out_per_group = params.c_out / params.groups;\n    let patch_columns = c_in_per_group * params.kernel_h * params.kernel_w;\n    let patch_rows = params.output_h * params.output_w;\n\n    if (params.execution == 0u) {\n        for (var b = 0u; b < params.batch; b++) {\n            for (var group = 0u; group < params.groups; group++) {\n                for (var row = 0u; row < patch_rows; row++) {\n                    let oh = row / params.output_w;\n                    let ow = row - oh * params.output_w;\n                    for (var n = 0u; n < c_out_per_group; n++) {\n                        let co = group * c_out_per_group + n;\n                        var acc = 0.0;\n                        for (var column = 0u; column < patch_columns; column++) {\n                            let kw = column % params.kernel_w;\n                            let rest = column / params.kernel_w;\n                            let kh = rest % params.kernel_h;\n                            let ci_local = rest / params.kernel_h;\n                            let channel = group * c_in_per_group + ci_local;\n                            acc += input_value(b, channel, oh, ow, kh, kw) * weight[co * patch_columns + column];\n                        }\n                        let output_index = ((b * params.c_out + co) * params.output_h + oh) * params.output_w + ow;\n                        result[output_index] = scale[co] * acc;\n                    }\n                }\n            }\n        }\n        return;\n    }\n\n    for (var b = 0u; b < params.batch; b++) {\n        for (var group = 0u; group < params.groups; group++) {\n            for (var row_start = 0u; row_start < patch_rows; row_start += 32u) {\n                let row_count = min(32u, patch_rows - row_start);\n                for (var local_row = 0u; local_row < row_count; local_row++) {\n                    let row = row_start + local_row;\n                    let oh = row / params.output_w;\n                    let ow = row - oh * params.output_w;\n                    for (var n = 0u; n < c_out_per_group; n++) {\n                        let co = group * c_out_per_group + n;\n                        let output_index = ((b * params.c_out + co) * params.output_h + oh) * params.output_w + ow;\n                        let gy = grad_output[output_index];\n                        let s = scale[co];\n                        var product = 0.0;\n                        for (var column = 0u; column < patch_columns; column++) {\n                            let kw = column % params.kernel_w;\n                            let rest = column / params.kernel_w;\n                            let kh = rest % params.kernel_h;\n                            let ci_local = rest / params.kernel_h;\n                            let channel = group * c_in_per_group + ci_local;\n                            let activation = input_value(b, channel, oh, ow, kh, kw);\n                            let weight_index = co * patch_columns + column;\n                            let w = weight[weight_index];\n                            product += activation * w;\n                            grad_weight[weight_index] += gy * s * activation;\n\n                            let ih = i32(oh * params.stride_h + kh * params.dilation_h) - i32(params.pad_top);\n                            let iw = i32(ow * params.stride_w + kw * params.dilation_w) - i32(params.pad_left);\n                            if (ih >= 0 && ih < i32(params.input_h) && iw >= 0 && iw < i32(params.input_w)) {\n                                let input_index = ((b * params.c_in + channel) * params.input_h + u32(ih)) * params.input_w + u32(iw);\n                                result[input_index] += gy * s * w;\n                            }\n                        }\n                        grad_scale[co] += gy * product;\n                    }\n                }\n            }\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([1,1,1]) as readonly [number, number, number]}),
  }),
  "embedding": Object.freeze({
    id: "embedding",
    sha256: "1e2dade24982855930404d9e39bb723660f0cb793d51aeb828dc858be649a725",
    source: "struct Params {\n    vocab: u32,\n    width: u32,\n    sequence: u32,\n    operation: u32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> weight: array<f32>;\n@group(0) @binding(2) var<storage, read> tokens: array<u32>;\n@group(0) @binding(3) var<storage, read> gradient: array<f32>;\n@group(0) @binding(4) var<storage, read_write> result: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (params.operation == 0u) {\n        if (index >= params.sequence * params.width) { return; }\n        let sequence_index = index / params.width;\n        let column = index % params.width;\n        result[index] = weight[tokens[sequence_index] * params.width + column];\n    } else {\n        if (index >= params.vocab * params.width) { return; }\n        let token = index / params.width;\n        let column = index % params.width;\n        var accumulator = 0.0;\n        for (var sequence_index = 0u; sequence_index < params.sequence;\n             sequence_index = sequence_index + 1u) {\n            if (tokens[sequence_index] == token) {\n                accumulator = accumulator\n                    + gradient[sequence_index * params.width + column];\n            }\n        }\n        result[index] = accumulator;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "fsq": Object.freeze({
    id: "fsq",
    sha256: "210c438e75a65c4f1ef14b4f49d4bda5a33a87ce31cd89508034ed3c38e29d57",
    source: "struct Params {\n    total: u32,\n    len: u32,\n    bound: u32,\n    estimator: u32,\n    execution: u32,\n    alpha: f32,\n    seed_low: u32,\n    seed_high: u32,\n};\n\nstruct U64 {\n    low: u32,\n    high: u32,\n};\n\nstruct NormalParts {\n    mantissa: u32,\n    exponent: i32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> x: array<f32>;\n@group(0) @binding(2) var<storage, read> levels: array<u32>;\n@group(0) @binding(3) var<storage, read> upstream: array<f32>;\n@group(0) @binding(4) var<storage, read_write> result: array<f32>;\n\nfn normal_parts(bits: u32) -> NormalParts {\n    let exponent_bits = (bits >> 23u) & 255u;\n    var mantissa = bits & 0x007fffffu;\n    var exponent = -126i;\n    if (exponent_bits != 0u) {\n        mantissa |= 0x00800000u;\n        exponent = i32(exponent_bits) - 127i;\n    } else {\n        loop {\n            if ((mantissa & 0x00800000u) != 0u) {\n                break;\n            }\n            mantissa <<= 1u;\n            exponent -= 1i;\n        }\n    }\n    return NormalParts(mantissa, exponent);\n}\n\nfn div_cr(numerator: f32, denominator: f32) -> f32 {\n    let numerator_bits = bitcast<u32>(numerator);\n    let denominator_bits = bitcast<u32>(denominator);\n    if ((numerator_bits & 0x7fffffffu) == 0u) {\n        return numerator;\n    }\n    let sign = (numerator_bits ^ denominator_bits) & 0x80000000u;\n    let left = normal_parts(numerator_bits & 0x7fffffffu);\n    let right = normal_parts(denominator_bits & 0x7fffffffu);\n    var exponent = left.exponent - right.exponent;\n    var dividend = left.mantissa;\n    if (dividend < right.mantissa) {\n        dividend <<= 1u;\n        exponent -= 1i;\n    }\n    var remainder = dividend - right.mantissa;\n    var quotient = 1u;\n    for (var bit = 0u; bit < 25u; bit += 1u) {\n        remainder <<= 1u;\n        quotient <<= 1u;\n        if (remainder >= right.mantissa) {\n            remainder -= right.mantissa;\n            quotient |= 1u;\n        }\n    }\n    var significand = quotient >> 2u;\n    let guard = (quotient & 2u) != 0u;\n    let round_or_sticky = (quotient & 1u) != 0u || remainder != 0u;\n    if (guard && (round_or_sticky || (significand & 1u) != 0u)) {\n        significand += 1u;\n    }\n    if (significand == 0x01000000u) {\n        significand >>= 1u;\n        exponent += 1i;\n    }\n    let biased = exponent + 127i;\n    if (biased <= 0i || biased >= 255i) {\n        return numerator / denominator;\n    }\n    return bitcast<f32>(sign | (u32(biased) << 23u) | (significand & 0x007fffffu));\n}\n\nfn add64(left: U64, right: U64) -> U64 {\n    let low = left.low + right.low;\n    let carry = select(0u, 1u, low < left.low);\n    return U64(low, left.high + right.high + carry);\n}\n\nfn shift_left_1(value: U64) -> U64 {\n    return U64(value.low << 1u, (value.high << 1u) | (value.low >> 31u));\n}\n\nfn shift_left_13(value: U64) -> U64 {\n    return U64(value.low << 13u, (value.high << 13u) | (value.low >> 19u));\n}\n\nfn shift_left_17(value: U64) -> U64 {\n    return U64(value.low << 17u, (value.high << 17u) | (value.low >> 15u));\n}\n\nfn shift_right_7(value: U64) -> U64 {\n    return U64((value.low >> 7u) | (value.high << 25u), value.high >> 7u);\n}\n\nfn multiply_index(index: u32) -> U64 {\n    var bits = index;\n    var addend = U64(0x7f4a7c15u, 0x9e3779b9u);\n    var product = U64(0u, 0u);\n    for (var position = 0u; position < 32u; position += 1u) {\n        if ((bits & 1u) != 0u) {\n            product = add64(product, addend);\n        }\n        bits >>= 1u;\n        addend = shift_left_1(addend);\n    }\n    return product;\n}\n\nfn modulo_million(value: U64) -> u32 {\n    var remainder = 0u;\n    for (var position = 63i; position >= 0i; position -= 1i) {\n        var bit = 0u;\n        if (position >= 32i) {\n            bit = (value.high >> u32(position - 32i)) & 1u;\n        } else {\n            bit = (value.low >> u32(position)) & 1u;\n        }\n        remainder = remainder * 2u + bit;\n        if (remainder >= 1000000u) {\n            remainder -= 1000000u;\n        }\n    }\n    return remainder;\n}\n\nfn sample_uniform(index: u32) -> f32 {\n    let product = multiply_index(index);\n    var state = U64(params.seed_low ^ product.low, params.seed_high ^ product.high);\n    state.low |= 1u;\n    let left13 = shift_left_13(state);\n    state = U64(state.low ^ left13.low, state.high ^ left13.high);\n    let right7 = shift_right_7(state);\n    state = U64(state.low ^ right7.low, state.high ^ right7.high);\n    let left17 = shift_left_17(state);\n    state = U64(state.low ^ left17.low, state.high ^ left17.high);\n    return div_cr(f32(modulo_million(state)), 1000000.0);\n}\n\nfn bound_value(value: f32) -> f32 {\n    return select(clamp(value, -1.0, 1.0), tanh(value), params.bound == 1u);\n}\n\nfn bound_derivative(value: f32) -> f32 {\n    if (params.bound == 0u) {\n        return select(0.0, 1.0, abs(value) < 1.0);\n    }\n    let bounded = tanh(value);\n    return 1.0 - bounded * bounded;\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index >= params.total) {\n        return;\n    }\n    if (params.execution == 0u) {\n        let maximum = f32(levels[index / params.len] - 1u);\n        let bounded = bound_value(x[index]);\n        var position = (bounded + 1.0) * 0.5 * maximum;\n        var code = 0.0;\n        if (params.estimator == 2u) {\n            position = clamp(position, 0.0, maximum);\n            let base = floor(position);\n            code = base + select(0.0, 1.0, sample_uniform(index) < position - base);\n        } else {\n            code = floor(position + 0.5);\n        }\n        code = clamp(code, 0.0, maximum);\n        result[index] = div_cr(code, maximum) * 2.0 - 1.0;\n    } else {\n        var derivative = bound_derivative(x[index]);\n        if (params.estimator == 1u) {\n            let maximum = f32(levels[index / params.len] - 1u);\n            let position = (bound_value(x[index]) + 1.0) * 0.5 * maximum;\n            derivative *= 1.0 - params.alpha * cos(6.283185307179586 * position);\n        }\n        result[index] = upstream[index] * derivative;\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "int8_adamw": Object.freeze({
    id: "int8_adamw",
    sha256: "25aed72c99a390bb3e0ca8aa54bcb6f8e55bad3cb662cfefa8170101a0f40570",
    source: "struct Params {\n    len: u32,\n    padding_0: u32,\n    padding_1: u32,\n    padding_2: u32,\n    learning_rate: f32,\n    beta1: f32,\n    beta2: f32,\n    epsilon: f32,\n    correction1: f32,\n    correction2: f32,\n    shrink: f32,\n    padding_3: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read_write> parameter: array<f32>;\n@group(0) @binding(2) var<storage, read> gradient: array<f32>;\n@group(0) @binding(3) var<storage, read_write> moment1: array<u32>;\n@group(0) @binding(4) var<storage, read_write> moment2: array<u32>;\n@group(0) @binding(5) var<storage, read_write> moment1_scale: array<f32>;\n@group(0) @binding(6) var<storage, read_write> moment2_scale: array<f32>;\n@group(0) @binding(7) var<storage, read_write> scratch1: array<f32>;\n@group(0) @binding(8) var<storage, read_write> scratch2: array<f32>;\n\nvar<workgroup> block_m: array<f32, 256>;\nvar<workgroup> block_v: array<f32, 256>;\n\nfn signed_code(code: u32) -> f32 {\n    if (code < 128u) {\n        return f32(code);\n    }\n    return f32(i32(code) - 256);\n}\n\n// Correctly-rounded binary32 square root. Integer restoring algorithm from\n// fdlibm/musl; round-to-nearest-even is explicit, independent of driver sqrt.\nfn sqrt_cr(value: f32) -> f32 {\n    var bits = bitcast<u32>(value);\n    if (bits == 0u) {\n        return value;\n    }\n    var exponent = i32(bits >> 23u);\n    if (exponent == 0) {\n        var shifts = 0i;\n        loop {\n            if ((bits & 0x00800000u) != 0u) {\n                break;\n            }\n            bits = bits << 1u;\n            shifts += 1i;\n        }\n        exponent -= shifts - 1i;\n    }\n    exponent -= 127i;\n    bits = (bits & 0x007fffffu) | 0x00800000u;\n    if ((exponent & 1i) != 0i) {\n        bits += bits;\n    }\n    exponent >>= 1u;\n    bits += bits;\n\n    var root = 0u;\n    var accumulator = 0u;\n    var moving = 0x01000000u;\n    loop {\n        if (moving == 0u) {\n            break;\n        }\n        let trial = accumulator + moving;\n        if (trial <= bits) {\n            accumulator = trial + moving;\n            bits -= trial;\n            root += moving;\n        }\n        bits += bits;\n        moving >>= 1u;\n    }\n    if (bits != 0u) {\n        root += root & 1u;\n    }\n    let mantissa = (root >> 1u) + 0x3f000000u;\n    return bitcast<f32>(mantissa + (u32(exponent) << 23u));\n}\n\nstruct NormalParts {\n    mantissa: u32,\n    exponent: i32,\n};\n\nfn normal_parts(bits: u32) -> NormalParts {\n    let exponent_bits = (bits >> 23u) & 255u;\n    var mantissa = bits & 0x007fffffu;\n    var exponent = -126i;\n    if (exponent_bits != 0u) {\n        mantissa |= 0x00800000u;\n        exponent = i32(exponent_bits) - 127i;\n    } else {\n        loop {\n            if ((mantissa & 0x00800000u) != 0u) {\n                break;\n            }\n            mantissa <<= 1u;\n            exponent -= 1i;\n        }\n    }\n    return NormalParts(mantissa, exponent);\n}\n\n// Correctly-rounded binary32 division for finite nonzero normal results.\n// Long division emits guard+round bits; remaining remainder is sticky.\nfn div_cr(numerator: f32, denominator: f32) -> f32 {\n    let numerator_bits = bitcast<u32>(numerator);\n    let denominator_bits = bitcast<u32>(denominator);\n    if ((numerator_bits & 0x7fffffffu) == 0u) {\n        return numerator;\n    }\n    let sign = (numerator_bits ^ denominator_bits) & 0x80000000u;\n    let left = normal_parts(numerator_bits & 0x7fffffffu);\n    let right = normal_parts(denominator_bits & 0x7fffffffu);\n    var exponent = left.exponent - right.exponent;\n    var dividend = left.mantissa;\n    if (dividend < right.mantissa) {\n        dividend <<= 1u;\n        exponent -= 1i;\n    }\n    var remainder = dividend - right.mantissa;\n    var quotient = 1u;\n    for (var bit = 0u; bit < 25u; bit += 1u) {\n        remainder <<= 1u;\n        quotient <<= 1u;\n        if (remainder >= right.mantissa) {\n            remainder -= right.mantissa;\n            quotient |= 1u;\n        }\n    }\n    var significand = quotient >> 2u;\n    let guard = (quotient & 2u) != 0u;\n    let round_or_sticky = (quotient & 1u) != 0u || remainder != 0u;\n    if (guard && (round_or_sticky || (significand & 1u) != 0u)) {\n        significand += 1u;\n    }\n    if (significand == 0x01000000u) {\n        significand >>= 1u;\n        exponent += 1i;\n    }\n    let biased = exponent + 127i;\n    if (biased <= 0i || biased >= 255i) {\n        return numerator / denominator;\n    }\n    return bitcast<f32>(sign | (u32(biased) << 23u) | (significand & 0x007fffffu));\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn dequantize(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let block = index / 256u;\n        moment1[index] = bitcast<u32>(signed_code(moment1[index]) * moment1_scale[block]);\n        moment2[index] = bitcast<u32>(f32(moment2[index]) * moment2_scale[block]);\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn square_variance(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let root = bitcast<f32>(moment2[index]);\n        moment2[index] = bitcast<u32>(root * root);\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn products(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let g = gradient[index];\n        moment1[index] = bitcast<u32>(params.beta1 * bitcast<f32>(moment1[index]));\n        scratch1[index] = (1.0 - params.beta1) * g;\n        moment2[index] = bitcast<u32>(params.beta2 * bitcast<f32>(moment2[index]));\n        scratch2[index] = (1.0 - params.beta2) * g;\n        parameter[index] = parameter[index] * params.shrink;\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn finish_products(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        moment1[index] = bitcast<u32>(bitcast<f32>(moment1[index]) + scratch1[index]);\n        scratch2[index] = scratch2[index] * gradient[index];\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn finish_variance(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        moment2[index] = bitcast<u32>(bitcast<f32>(moment2[index]) + scratch2[index]);\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn update_parameter(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let m = bitcast<f32>(moment1[index]);\n        let v = bitcast<f32>(moment2[index]);\n        let corrected_m = div_cr(m, params.correction1);\n        let corrected_v = div_cr(v, params.correction2);\n        let adaptive = params.learning_rate\n            * div_cr(corrected_m, sqrt_cr(corrected_v) + params.epsilon);\n        parameter[index] = parameter[index] - adaptive;\n    }\n}\n\n@compute @workgroup_size(256, 1, 1)\nfn reduce_scales(\n    @builtin(workgroup_id) group: vec3<u32>,\n    @builtin(local_invocation_id) local: vec3<u32>,\n) {\n    let index = group.x * 256u + local.x;\n    var m = 0.0;\n    var root = 0.0;\n    if (index < params.len) {\n        m = abs(bitcast<f32>(moment1[index]));\n        root = sqrt_cr(bitcast<f32>(moment2[index]));\n        moment2[index] = bitcast<u32>(root);\n    }\n    block_m[local.x] = m;\n    block_v[local.x] = root;\n    workgroupBarrier();\n\n    var stride = 128u;\n    loop {\n        if (local.x < stride) {\n            block_m[local.x] = max(block_m[local.x], block_m[local.x + stride]);\n            block_v[local.x] = max(block_v[local.x], block_v[local.x + stride]);\n        }\n        workgroupBarrier();\n        if (stride == 1u) {\n            break;\n        }\n        stride = stride / 2u;\n    }\n    if (local.x == 0u) {\n        moment1_scale[group.x] = select(0.0, div_cr(block_m[0], 127.0), block_m[0] > 0.0);\n        moment2_scale[group.x] = select(0.0, div_cr(block_v[0], 255.0), block_v[0] > 0.0);\n    }\n}\n\n@compute @workgroup_size(64, 1, 1)\nfn quantize(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index < params.len) {\n        let block = index / 256u;\n        let m_scale = moment1_scale[block];\n        let v_scale = moment2_scale[block];\n        let m = bitcast<f32>(moment1[index]);\n        let root = bitcast<f32>(moment2[index]);\n        if (m_scale > 0.0) {\n            let code = i32(clamp(round(div_cr(m, m_scale)), -127.0, 127.0));\n            moment1[index] = u32(code) & 255u;\n        } else {\n            moment1[index] = 0u;\n        }\n        if (v_scale > 0.0 && root > 0.0) {\n            moment2[index] = u32(clamp(round(div_cr(root, v_scale)), 1.0, 255.0));\n        } else {\n            moment2[index] = 0u;\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"dequantize": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"})]),"square_variance": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),"products": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),"finish_products": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":7,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),"finish_variance": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":8,"addressSpace":"storage","access":"read_write"})]),"update_parameter": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),"reduce_scales": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"})]),"quantize": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":6,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"dequantize": Object.freeze([64,1,1]) as readonly [number, number, number],"square_variance": Object.freeze([64,1,1]) as readonly [number, number, number],"products": Object.freeze([64,1,1]) as readonly [number, number, number],"finish_products": Object.freeze([64,1,1]) as readonly [number, number, number],"finish_variance": Object.freeze([64,1,1]) as readonly [number, number, number],"update_parameter": Object.freeze([64,1,1]) as readonly [number, number, number],"reduce_scales": Object.freeze([256,1,1]) as readonly [number, number, number],"quantize": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "muon": Object.freeze({
    id: "muon",
    sha256: "9e9a9790c1e43c12a291a896a9a893410cd9c091499aa15742ddf445243a5dfe",
    source: "struct Params {\n    len: u32,\n    rows: u32,\n    cols: u32,\n    steps: u32,\n    momentum_decay: f32,\n    scale: f32,\n    shrink: f32,\n    padding: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read_write> parameter: array<f32>;\n@group(0) @binding(2) var<storage, read> gradient: array<f32>;\n@group(0) @binding(3) var<storage, read_write> momentum: array<f32>;\n@group(0) @binding(4) var<storage, read_write> workspace: array<f32>;\n\nfn sqrt_cr(value: f32) -> f32 {\n    var bits = bitcast<u32>(value);\n    if (bits == 0u) {\n        return value;\n    }\n    var exponent = i32(bits >> 23u);\n    if (exponent == 0) {\n        var shifts = 0i;\n        loop {\n            if ((bits & 0x00800000u) != 0u) {\n                break;\n            }\n            bits <<= 1u;\n            shifts += 1i;\n        }\n        exponent -= shifts - 1i;\n    }\n    exponent -= 127i;\n    bits = (bits & 0x007fffffu) | 0x00800000u;\n    if ((exponent & 1i) != 0i) {\n        bits += bits;\n    }\n    exponent >>= 1u;\n    bits += bits;\n    var root = 0u;\n    var accumulator = 0u;\n    var moving = 0x01000000u;\n    loop {\n        if (moving == 0u) {\n            break;\n        }\n        let trial = accumulator + moving;\n        if (trial <= bits) {\n            accumulator = trial + moving;\n            bits -= trial;\n            root += moving;\n        }\n        bits += bits;\n        moving >>= 1u;\n    }\n    if (bits != 0u) {\n        root += root & 1u;\n    }\n    return bitcast<f32>((root >> 1u) + 0x3f000000u + (u32(exponent) << 23u));\n}\n\nstruct NormalParts {\n    mantissa: u32,\n    exponent: i32,\n};\n\nfn normal_parts(bits: u32) -> NormalParts {\n    let exponent_bits = (bits >> 23u) & 255u;\n    var mantissa = bits & 0x007fffffu;\n    var exponent = -126i;\n    if (exponent_bits != 0u) {\n        mantissa |= 0x00800000u;\n        exponent = i32(exponent_bits) - 127i;\n    } else {\n        loop {\n            if ((mantissa & 0x00800000u) != 0u) {\n                break;\n            }\n            mantissa <<= 1u;\n            exponent -= 1i;\n        }\n    }\n    return NormalParts(mantissa, exponent);\n}\n\nfn div_cr(numerator: f32, denominator: f32) -> f32 {\n    let numerator_bits = bitcast<u32>(numerator);\n    let denominator_bits = bitcast<u32>(denominator);\n    if ((numerator_bits & 0x7fffffffu) == 0u) {\n        return numerator;\n    }\n    let sign = (numerator_bits ^ denominator_bits) & 0x80000000u;\n    let left = normal_parts(numerator_bits & 0x7fffffffu);\n    let right = normal_parts(denominator_bits & 0x7fffffffu);\n    var exponent = left.exponent - right.exponent;\n    var dividend = left.mantissa;\n    if (dividend < right.mantissa) {\n        dividend <<= 1u;\n        exponent -= 1i;\n    }\n    var remainder = dividend - right.mantissa;\n    var quotient = 1u;\n    for (var bit = 0u; bit < 25u; bit += 1u) {\n        remainder <<= 1u;\n        quotient <<= 1u;\n        if (remainder >= right.mantissa) {\n            remainder -= right.mantissa;\n            quotient |= 1u;\n        }\n    }\n    var significand = quotient >> 2u;\n    let guard = (quotient & 2u) != 0u;\n    let round_or_sticky = (quotient & 1u) != 0u || remainder != 0u;\n    if (guard && (round_or_sticky || (significand & 1u) != 0u)) {\n        significand += 1u;\n    }\n    if (significand == 0x01000000u) {\n        significand >>= 1u;\n        exponent += 1i;\n    }\n    let biased = exponent + 127i;\n    if (biased <= 0i || biased >= 255i) {\n        return numerator / denominator;\n    }\n    return bitcast<f32>(sign | (u32(biased) << 23u) | (significand & 0x007fffffu));\n}\n\n@compute @workgroup_size(1, 1, 1)\nfn main() {\n    let n = params.len;\n    let r = min(params.rows, params.cols);\n    let c = max(params.rows, params.cols);\n    let square = r * r;\n    let x_offset = 0u;\n    let xt_offset = n;\n    let bx_offset = 2u * n;\n    let gram_offset = 3u * n;\n    let gram2_offset = gram_offset + square;\n    let bmat_offset = gram2_offset + square;\n    let temporary = bmat_offset + square;\n\n    for (var index = 0u; index < n; index += 1u) {\n        workspace[temporary] = params.momentum_decay * momentum[index];\n        storageBarrier();\n        momentum[index] = workspace[temporary] + gradient[index];\n    }\n    storageBarrier();\n    for (var index = 0u; index < n; index += 1u) {\n        workspace[bx_offset + index] = momentum[index] * momentum[index];\n    }\n    storageBarrier();\n    var norm2 = 0.0;\n    for (var index = 0u; index < n; index += 1u) {\n        norm2 += workspace[bx_offset + index];\n    }\n    let fnorm = sqrt_cr(norm2) + 1.0e-7;\n    let transposed = params.rows > params.cols;\n    for (var row = 0u; row < r; row += 1u) {\n        for (var col = 0u; col < c; col += 1u) {\n            let source = select(row * params.cols + col, col * params.cols + row, transposed);\n            workspace[x_offset + row * c + col] = div_cr(momentum[source], fnorm);\n        }\n    }\n\n    for (var iteration = 0u; iteration < params.steps; iteration += 1u) {\n        storageBarrier();\n        for (var row = 0u; row < r; row += 1u) {\n            for (var col = 0u; col < r; col += 1u) {\n                var sum = 0.0;\n                for (var k = 0u; k < c; k += 1u) {\n                    workspace[temporary] = workspace[x_offset + row * c + k]\n                        * workspace[x_offset + col * c + k];\n                    storageBarrier();\n                    sum += workspace[temporary];\n                }\n                workspace[gram_offset + row * r + col] = sum;\n            }\n        }\n        storageBarrier();\n        for (var row = 0u; row < r; row += 1u) {\n            for (var col = 0u; col < r; col += 1u) {\n                var sum = 0.0;\n                for (var k = 0u; k < r; k += 1u) {\n                    workspace[temporary] = workspace[gram_offset + row * r + k]\n                        * workspace[gram_offset + col * r + k];\n                    storageBarrier();\n                    sum += workspace[temporary];\n                }\n                workspace[gram2_offset + row * r + col] = sum;\n            }\n        }\n        storageBarrier();\n        for (var index = 0u; index < square; index += 1u) {\n            workspace[temporary] = -4.775 * workspace[gram_offset + index];\n            workspace[temporary + 1u] = 2.0315 * workspace[gram2_offset + index];\n            storageBarrier();\n            workspace[bmat_offset + index] = workspace[temporary] + workspace[temporary + 1u];\n        }\n        for (var row = 0u; row < c; row += 1u) {\n            for (var col = 0u; col < r; col += 1u) {\n                workspace[xt_offset + row * r + col] = workspace[x_offset + col * c + row];\n            }\n        }\n        storageBarrier();\n        for (var row = 0u; row < r; row += 1u) {\n            for (var col = 0u; col < c; col += 1u) {\n                var sum = 0.0;\n                for (var k = 0u; k < r; k += 1u) {\n                    workspace[temporary] = workspace[bmat_offset + row * r + k]\n                        * workspace[xt_offset + col * r + k];\n                    storageBarrier();\n                    sum += workspace[temporary];\n                }\n                workspace[bx_offset + row * c + col] = sum;\n            }\n        }\n        storageBarrier();\n        for (var index = 0u; index < n; index += 1u) {\n            workspace[temporary] = 3.4445 * workspace[x_offset + index];\n            storageBarrier();\n            workspace[x_offset + index] = workspace[temporary] + workspace[bx_offset + index];\n        }\n    }\n\n    storageBarrier();\n    for (var row = 0u; row < params.rows; row += 1u) {\n        for (var col = 0u; col < params.cols; col += 1u) {\n            let index = row * params.cols + col;\n            let ortho = select(index, col * c + row, transposed);\n            workspace[temporary] = parameter[index] * params.shrink;\n            workspace[temporary + 1u] = params.scale * workspace[x_offset + ortho];\n            storageBarrier();\n            parameter[index] = workspace[temporary] - workspace[temporary + 1u];\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([1,1,1]) as readonly [number, number, number]}),
  }),
  "pointwise": Object.freeze({
    id: "pointwise",
    sha256: "ac7585afd2c92a71ab21394985374ea21953dc0c190d2baca86f944d5d26f88a",
    source: "struct Params {\n    len: u32,\n    operation: u32,\n    scalar: f32,\n    auxiliary: u32,\n    secondary: u32,\n    tertiary: u32,\n    padding_0: u32,\n    padding_1: u32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> left: array<f32>;\n@group(0) @binding(2) var<storage, read> right: array<f32>;\n@group(0) @binding(3) var<storage, read> extra: array<f32>;\n@group(0) @binding(4) var<storage, read_write> result: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let index = gid.x;\n    if (index >= params.len) {\n        return;\n    }\n    switch params.operation {\n        case 0u: { result[index] = left[index]; }\n        case 1u: { result[index] = 0.0; }\n        case 2u: { result[index] = left[index] * params.scalar; }\n        case 3u: { result[index] = left[index] + right[index]; }\n        case 4u: { result[index] = left[index] * right[index]; }\n        case 5u: {\n            let positive = max(left[index], 0.0);\n            result[index] = positive * positive;\n        }\n        case 6u: { result[index] = right[index] * 2.0 * max(left[index], 0.0); }\n        case 7u: {\n            let sigmoid = 1.0 / (1.0 + exp(-left[index]));\n            result[index] = left[index] * sigmoid;\n        }\n        case 8u: {\n            let sigmoid = 1.0 / (1.0 + exp(-left[index]));\n            result[index] = right[index]\n                * (sigmoid + left[index] * sigmoid * (1.0 - sigmoid));\n        }\n        case 9u: {\n            let row = index / params.auxiliary;\n            let column = index % params.auxiliary;\n            result[index] = select(-1e30, left[index], column <= row);\n        }\n        case 10u: {\n            let row = index / params.auxiliary;\n            let column = index % params.auxiliary;\n            result[index] = select(0.0, left[index], column <= row);\n        }\n        case 11u: {\n            let rows = params.len / params.auxiliary;\n            if (index >= rows) { return; }\n            let base = index * params.auxiliary;\n            var maximum = -3.402823466e38;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                maximum = max(maximum, left[base + column]);\n            }\n            var sum = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                let exponential = exp(left[base + column] - maximum);\n                result[base + column] = exponential;\n                sum = sum + exponential;\n            }\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                result[base + column] = result[base + column] / sum;\n            }\n        }\n        case 12u: {\n            let rows = params.len / params.auxiliary;\n            if (index >= rows) { return; }\n            let base = index * params.auxiliary;\n            var maximum = -3.402823466e38;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                maximum = max(maximum, left[base + column]);\n            }\n            var sum = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                sum = sum + exp(left[base + column] - maximum);\n            }\n            var contraction = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                let probability = exp(left[base + column] - maximum) / sum;\n                contraction = contraction + probability * right[base + column];\n            }\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                let probability = exp(left[base + column] - maximum) / sum;\n                result[base + column] = probability * (right[base + column] - contraction);\n            }\n        }\n        case 13u: {\n            let rows = params.len / params.auxiliary;\n            if (index >= rows) { return; }\n            let base = index * params.auxiliary;\n            var mean_square = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                let value = left[base + column];\n                mean_square = mean_square + value * value;\n            }\n            mean_square = mean_square / f32(params.auxiliary);\n            let inverse = 1.0 / sqrt(mean_square + params.scalar);\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                result[base + column] = left[base + column] * inverse * right[column];\n            }\n        }\n        case 14u: {\n            let rows = params.len / params.auxiliary;\n            if (index >= rows) { return; }\n            let base = index * params.auxiliary;\n            var mean_square = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                let value = left[base + column];\n                mean_square = mean_square + value * value;\n            }\n            mean_square = mean_square / f32(params.auxiliary);\n            let inverse = 1.0 / sqrt(mean_square + params.scalar);\n            var contraction = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                contraction = contraction\n                    + extra[base + column] * right[column] * left[base + column];\n            }\n            let correction = inverse * inverse * inverse * contraction\n                / f32(params.auxiliary);\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                result[base + column] = inverse * extra[base + column] * right[column]\n                    - correction * left[base + column];\n            }\n        }\n        case 15u: {\n            if (index >= params.auxiliary) { return; }\n            let rows = params.len / params.auxiliary;\n            var gradient = 0.0;\n            for (var row = 0u; row < rows; row = row + 1u) {\n                let base = row * params.auxiliary;\n                var mean_square = 0.0;\n                for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                    let value = left[base + column];\n                    mean_square = mean_square + value * value;\n                }\n                mean_square = mean_square / f32(params.auxiliary);\n                let inverse = 1.0 / sqrt(mean_square + params.scalar);\n                gradient = gradient + extra[base + index] * left[base + index] * inverse;\n            }\n            result[index] = gradient;\n        }\n        case 16u: {\n            if (index != 0u) { return; }\n            var loss = 0.0;\n            for (var item = 0u; item < params.len; item = item + 1u) {\n                let difference = left[item] - right[item];\n                loss = loss + difference * difference;\n            }\n            result[0] = loss / f32(params.len);\n        }\n        case 17u: {\n            result[index] = extra[0] * 2.0 * (left[index] - right[index])\n                / f32(params.len);\n        }\n        case 18u: { result[index] = left[index] + right[index % params.auxiliary]; }\n        case 19u: { result[index] = extra[index]; }\n        case 20u: {\n            if (index >= params.auxiliary) { return; }\n            let rows = params.len / params.auxiliary;\n            var gradient = 0.0;\n            for (var row = 0u; row < rows; row = row + 1u) {\n                gradient = gradient + extra[row * params.auxiliary + index];\n            }\n            result[index] = gradient;\n        }\n        case 21u: { result[index] = left[index] - params.scalar * right[index]; }\n        case 22u: {\n            let rows = params.len / params.auxiliary;\n            result[index] = left[(index % rows) * params.auxiliary + index / rows];\n        }\n        case 23u: {\n            let rows = params.len / params.auxiliary;\n            result[index] = left[(index % params.auxiliary) * rows + index / params.auxiliary];\n        }\n        case 24u: {\n            let sliced_columns = params.tertiary;\n            let row = index / sliced_columns;\n            let column = index % sliced_columns;\n            result[index] = left[row * params.auxiliary + params.secondary + column];\n        }\n        case 25u: {\n            let row = index / params.auxiliary;\n            let column = index % params.auxiliary;\n            if (column >= params.secondary && column < params.secondary + params.tertiary) {\n                result[index] = left[row * params.tertiary + column - params.secondary];\n            } else {\n                result[index] = 0.0;\n            }\n        }\n        case 26u: {\n            let row = index / params.secondary;\n            let output_column = index % params.secondary;\n            var accumulator = 0.0;\n            for (var inner = 0u; inner < params.tertiary; inner = inner + 1u) {\n                accumulator = accumulator\n                    + left[row * params.tertiary + inner]\n                    * right[output_column * params.tertiary + inner];\n            }\n            result[index] = accumulator;\n        }\n        case 27u: {\n            let row = index / params.tertiary;\n            let inner = index % params.tertiary;\n            var accumulator = 0.0;\n            for (var output_column = 0u; output_column < params.secondary;\n                 output_column = output_column + 1u) {\n                accumulator = accumulator\n                    + extra[row * params.secondary + output_column]\n                    * right[output_column * params.tertiary + inner];\n            }\n            result[index] = accumulator;\n        }\n        case 28u: {\n            let output_column = index / params.tertiary;\n            let inner = index % params.tertiary;\n            var accumulator = 0.0;\n            for (var row = 0u; row < params.auxiliary; row = row + 1u) {\n                accumulator = accumulator\n                    + extra[row * params.secondary + output_column]\n                    * left[row * params.tertiary + inner];\n            }\n            result[index] = accumulator;\n        }\n        case 29u: {\n            let row = index / params.secondary;\n            let output_column = index % params.secondary;\n            var accumulator = 0.0;\n            for (var inner = 0u; inner < params.tertiary; inner = inner + 1u) {\n                accumulator = accumulator\n                    + left[row * params.tertiary + inner]\n                    * right[output_column * params.tertiary + inner];\n            }\n            result[index] = extra[output_column] * accumulator;\n        }\n        case 30u: {\n            let row = index / params.tertiary;\n            let inner = index % params.tertiary;\n            var accumulator = 0.0;\n            for (var output_column = 0u; output_column < params.secondary;\n                 output_column = output_column + 1u) {\n                accumulator = accumulator\n                    + left[row * params.secondary + output_column]\n                    * extra[output_column]\n                    * right[output_column * params.tertiary + inner];\n            }\n            result[index] = accumulator;\n        }\n        case 31u: {\n            let output_column = index / params.tertiary;\n            let inner = index % params.tertiary;\n            var accumulator = 0.0;\n            for (var row = 0u; row < params.auxiliary; row = row + 1u) {\n                accumulator = accumulator\n                    + left[row * params.secondary + output_column]\n                    * extra[output_column]\n                    * right[row * params.tertiary + inner];\n            }\n            result[index] = accumulator;\n        }\n        case 32u: {\n            let output_column = index;\n            var gradient = 0.0;\n            for (var row = 0u; row < params.auxiliary; row = row + 1u) {\n                var contraction = 0.0;\n                for (var inner = 0u; inner < params.tertiary; inner = inner + 1u) {\n                    contraction = contraction\n                        + right[row * params.tertiary + inner]\n                        * extra[output_column * params.tertiary + inner];\n                }\n                gradient = gradient\n                    + left[row * params.secondary + output_column] * contraction;\n            }\n            result[index] = gradient;\n        }\n        case 33u: {\n            let row = index / params.auxiliary;\n            let row_scale = right[row];\n            if (row_scale == 0.0) {\n                result[index] = 0.0;\n            } else {\n                result[index] = clamp(left[index] / row_scale, -1.0, 1.0);\n            }\n        }\n        case 34u: {\n            let row = index / params.auxiliary;\n            let row_scale = right[row];\n            if (row_scale != 0.0 && abs(left[index] / row_scale) < 1.0) {\n                result[index] = extra[index] / row_scale;\n            } else {\n                result[index] = 0.0;\n            }\n        }\n        case 35u: {\n            let row = index / params.auxiliary;\n            let row_alpha = right[row];\n            if (row_alpha <= 0.0) {\n                result[index] = 0.0;\n            } else {\n                result[index] = clamp(round(left[index] / row_alpha), -1.0, 1.0)\n                    * row_alpha;\n            }\n        }\n        case 36u: {\n            let row = index / params.auxiliary;\n            let row_alpha = right[row];\n            if (row_alpha > 0.0 && abs(left[index] / row_alpha) < 1.0) {\n                result[index] = extra[index];\n            } else {\n                result[index] = 0.0;\n            }\n        }\n        case 37u: {\n            let row = index;\n            let row_alpha = right[row];\n            if (row_alpha <= 0.0) {\n                result[index] = 0.0;\n                return;\n            }\n            var gradient = 0.0;\n            for (var column = 0u; column < params.auxiliary; column = column + 1u) {\n                let element = row * params.auxiliary + column;\n                let normalized = left[element] / row_alpha;\n                var local: f32;\n                if (abs(normalized) < 1.0) {\n                    local = round(normalized) - normalized;\n                } else {\n                    local = sign(normalized);\n                }\n                gradient = gradient + extra[element] * local;\n            }\n            result[index] = gradient / sqrt(f32(params.auxiliary));\n        }\n        default: { result[index] = 0.0; }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "rope": Object.freeze({
    id: "rope",
    sha256: "7a82d78dce54676965083d144bdaa79950785d8510a0523ce0d8ddee3503ee5e",
    source: "struct Params {\n    n_token: u32,\n    n_head: u32,\n    head_dim: u32,\n    inverse: u32,\n    theta: f32,\n    padding_0: f32,\n    padding_1: f32,\n    padding_2: f32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> x: array<f32>;\n@group(0) @binding(2) var<storage, read> positions: array<u32>;\n@group(0) @binding(3) var<storage, read_write> result: array<f32>;\n\n@compute @workgroup_size(64, 1, 1)\nfn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n    let half = params.head_dim / 2u;\n    let total = params.n_token * params.n_head * half;\n    let pair = gid.x;\n    if (pair >= total) {\n        return;\n    }\n    let j = pair % half;\n    let head = (pair / half) % params.n_head;\n    let token = pair / (params.n_head * half);\n    let base = (token * params.n_head + head) * params.head_dim;\n    let exponent = -2.0 * f32(j) / f32(params.head_dim);\n    let angle = f32(positions[token]) * pow(params.theta, exponent);\n    let cosine = cos(angle);\n    let sine_sign = select(1.0, -1.0, params.inverse != 0u);\n    let sine = sine_sign * sin(angle);\n    let a = x[base + j];\n    let b = x[base + j + half];\n    result[base + j] = a * cosine - b * sine;\n    result[base + j + half] = b * cosine + a * sine;\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([64,1,1]) as readonly [number, number, number]}),
  }),
  "salt": Object.freeze({
    id: "salt",
    sha256: "03363e693376a44f1f33369464621535b50ab85ec19c0809a84f5e0e49f02ff3",
    source: "struct Params {\n    rows: u32,\n    cols: u32,\n    planes: u32,\n    padding: u32,\n};\n\nstruct NormalParts {\n    mantissa: u32,\n    exponent: i32,\n};\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> weight: array<f32>;\n@group(0) @binding(2) var<storage, read_write> residual: array<f32>;\n@group(0) @binding(3) var<storage, read_write> result: array<f32>;\n\nfn normal_parts(bits: u32) -> NormalParts {\n    let exponent_bits = (bits >> 23u) & 255u;\n    var mantissa = bits & 0x007fffffu;\n    var exponent = -126i;\n    if (exponent_bits != 0u) {\n        mantissa |= 0x00800000u;\n        exponent = i32(exponent_bits) - 127i;\n    } else {\n        loop {\n            if ((mantissa & 0x00800000u) != 0u) {\n                break;\n            }\n            mantissa <<= 1u;\n            exponent -= 1i;\n        }\n    }\n    return NormalParts(mantissa, exponent);\n}\n\nfn div_cr(numerator: f32, denominator: f32) -> f32 {\n    let numerator_bits = bitcast<u32>(numerator);\n    let denominator_bits = bitcast<u32>(denominator);\n    if ((numerator_bits & 0x7fffffffu) == 0u) {\n        return numerator;\n    }\n    let sign = (numerator_bits ^ denominator_bits) & 0x80000000u;\n    let left = normal_parts(numerator_bits & 0x7fffffffu);\n    let right = normal_parts(denominator_bits & 0x7fffffffu);\n    var exponent = left.exponent - right.exponent;\n    var dividend = left.mantissa;\n    if (dividend < right.mantissa) {\n        dividend <<= 1u;\n        exponent -= 1i;\n    }\n    var remainder = dividend - right.mantissa;\n    var quotient = 1u;\n    for (var bit = 0u; bit < 25u; bit += 1u) {\n        remainder <<= 1u;\n        quotient <<= 1u;\n        if (remainder >= right.mantissa) {\n            remainder -= right.mantissa;\n            quotient |= 1u;\n        }\n    }\n    var significand = quotient >> 2u;\n    let guard = (quotient & 2u) != 0u;\n    let round_or_sticky = (quotient & 1u) != 0u || remainder != 0u;\n    if (guard && (round_or_sticky || (significand & 1u) != 0u)) {\n        significand += 1u;\n    }\n    if (significand == 0x01000000u) {\n        significand >>= 1u;\n        exponent += 1i;\n    }\n    let biased = exponent + 127i;\n    if (biased <= 0i || biased >= 255i) {\n        return numerator / denominator;\n    }\n    return bitcast<f32>(sign | (u32(biased) << 23u) | (significand & 0x007fffffu));\n}\n\nfn round_away(value: f32) -> f32 {\n    return select(ceil(value - 0.5), floor(value + 0.5), value >= 0.0);\n}\n\n@compute @workgroup_size(1, 1, 1)\nfn main() {\n    for (var row = 0u; row < params.rows; row += 1u) {\n        let base = row * params.cols;\n        for (var column = 0u; column < params.cols; column += 1u) {\n            residual[column] = weight[base + column];\n            result[base + column] = 0.0;\n        }\n        for (var plane = 0u; plane < params.planes; plane += 1u) {\n            var sum = 0.0;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                sum += abs(residual[column]);\n            }\n            let scale = div_cr(sum, f32(params.cols));\n            if (scale != 0.0) {\n                for (var column = 0u; column < params.cols; column += 1u) {\n                    let index = base + column;\n                    let old = residual[column];\n                    let trit = clamp(round_away(div_cr(old, scale)), -1.0, 1.0);\n                    residual[column] = scale * trit;\n                    result[index] = result[index] + residual[column];\n                    residual[column] = old - residual[column];\n                }\n            }\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read_write"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([1,1,1]) as readonly [number, number, number]}),
  }),
  "softmax_xent": Object.freeze({
    id: "softmax_xent",
    sha256: "90ff3aaeae3fba70fdbce1e08623d792bb8442ae68a765c806dc0ba6c31682ce",
    source: "struct Params {\n    rows: u32,\n    cols: u32,\n    execution: u32,\n    padding: u32,\n    padding_4: f32,\n    padding_1: f32,\n    padding_2: f32,\n    padding_3: f32,\n};\n\n// Chrome Tint rejects a bitcast infinity literal. This finite sentinel still\n// underflows through exp() and preserves masked-softmax behavior.\nconst NEGATIVE_LARGE: f32 = -3.402823466e+38;\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> logits: array<f32>;\n@group(0) @binding(2) var<storage, read> targets: array<f32>;\n@group(0) @binding(3) var<storage, read> grad_output: array<f32>;\n@group(0) @binding(4) var<storage, read_write> result: array<f32>;\n\n@compute @workgroup_size(1, 1, 1)\nfn main() {\n    if (params.execution == 0u) {\n        var total = 0.0;\n        for (var row = 0u; row < params.rows; row += 1u) {\n            let base = row * params.cols;\n            var maximum = NEGATIVE_LARGE;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                maximum = max(maximum, logits[base + column]);\n            }\n            var sum = 0.0;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                sum += exp(logits[base + column] - maximum);\n            }\n            for (var column = 0u; column < params.cols; column += 1u) {\n                let probability = exp(logits[base + column] - maximum) / sum;\n                total -= targets[base + column] * log(max(probability, 1.17549435e-38));\n            }\n        }\n        result[0] = total / f32(params.rows);\n    } else {\n        for (var row = 0u; row < params.rows; row += 1u) {\n            let base = row * params.cols;\n            var maximum = NEGATIVE_LARGE;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                maximum = max(maximum, logits[base + column]);\n            }\n            var sum = 0.0;\n            var target_sum = 0.0;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                sum += exp(logits[base + column] - maximum);\n                target_sum += targets[base + column];\n            }\n            for (var column = 0u; column < params.cols; column += 1u) {\n                let probability = exp(logits[base + column] - maximum) / sum;\n                result[base + column] = (grad_output[0] / f32(params.rows))\n                    * (probability * target_sum - targets[base + column]);\n            }\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([1,1,1]) as readonly [number, number, number]}),
  }),
  "topk_kd": Object.freeze({
    id: "topk_kd",
    sha256: "035df79a15835243fae2d244d6df7bde9f4bbd22167e7fa20ff1ee8fdeb445cc",
    source: "struct Params {\n    rows: u32,\n    cols: u32,\n    k: u32,\n    execution: u32,\n    padding_0: f32,\n    padding_1: f32,\n    padding_2: f32,\n    padding_3: f32,\n};\n\n// Chrome Tint rejects a bitcast infinity literal. This finite sentinel still\n// underflows through exp() and preserves masked-softmax behavior.\nconst NEGATIVE_LARGE: f32 = -3.402823466e+38;\n\n@group(0) @binding(0) var<uniform> params: Params;\n@group(0) @binding(1) var<storage, read> logits: array<f32>;\n@group(0) @binding(2) var<storage, read> indices: array<u32>;\n@group(0) @binding(3) var<storage, read> probabilities: array<f32>;\n@group(0) @binding(4) var<storage, read> grad_output: array<f32>;\n@group(0) @binding(5) var<storage, read_write> result: array<f32>;\n\n@compute @workgroup_size(1, 1, 1)\nfn main() {\n    if (params.execution == 0u) {\n        var total = 0.0;\n        for (var row = 0u; row < params.rows; row += 1u) {\n            let dense_base = row * params.cols;\n            let sparse_base = row * params.k;\n            var maximum = NEGATIVE_LARGE;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                maximum = max(maximum, logits[dense_base + column]);\n            }\n            var sum = 0.0;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                sum += exp(logits[dense_base + column] - maximum);\n            }\n            for (var sparse_column = 0u; sparse_column < params.k; sparse_column += 1u) {\n                let column = indices[sparse_base + sparse_column];\n                let probability = exp(logits[dense_base + column] - maximum) / sum;\n                total -= probabilities[sparse_base + sparse_column]\n                    * log(max(probability, 1.17549435e-38));\n            }\n        }\n        result[0] = total / f32(params.rows);\n    } else {\n        for (var row = 0u; row < params.rows; row += 1u) {\n            let dense_base = row * params.cols;\n            let sparse_base = row * params.k;\n            var maximum = NEGATIVE_LARGE;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                maximum = max(maximum, logits[dense_base + column]);\n            }\n            var sum = 0.0;\n            for (var column = 0u; column < params.cols; column += 1u) {\n                sum += exp(logits[dense_base + column] - maximum);\n            }\n            var probability_sum = 0.0;\n            for (var sparse_column = 0u; sparse_column < params.k; sparse_column += 1u) {\n                probability_sum += probabilities[sparse_base + sparse_column];\n            }\n            let scale = grad_output[0] / f32(params.rows);\n            for (var column = 0u; column < params.cols; column += 1u) {\n                let probability = exp(logits[dense_base + column] - maximum) / sum;\n                result[dense_base + column] = scale * probability * probability_sum;\n            }\n            for (var sparse_column = 0u; sparse_column < params.k; sparse_column += 1u) {\n                let column = indices[sparse_base + sparse_column];\n                result[dense_base + column] -=\n                    scale * probabilities[sparse_base + sparse_column];\n            }\n        }\n    }\n}\n",
    bindings: Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"})]),
    entryPointBindings: Object.freeze({"main": Object.freeze([Object.freeze({"group":0,"binding":0,"addressSpace":"uniform","access":null}),Object.freeze({"group":0,"binding":1,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":2,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":3,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":4,"addressSpace":"storage","access":"read"}),Object.freeze({"group":0,"binding":5,"addressSpace":"storage","access":"read_write"})])}),
    entryPoints: Object.freeze({"main": Object.freeze([1,1,1]) as readonly [number, number, number]}),
  }),
});

export const WEBGPU_OPERATION_MODULE_DEPENDENCIES_V1 = Object.freeze({
  "graph.ste_surrogate": Object.freeze(["pointwise"]),
  "graph.salt_ste": Object.freeze(["salt","pointwise"]),
  "graph.lsq_ste": Object.freeze(["pointwise"]),
  "graph.fsq": Object.freeze(["fsq"]),
  "graph.dense_matmul": Object.freeze(["pointwise"]),
  "graph.ternary_matmul": Object.freeze(["pointwise"]),
  "graph.transpose": Object.freeze(["pointwise"]),
  "graph.embedding_gather": Object.freeze(["embedding"]),
  "graph.slice_cols": Object.freeze(["pointwise"]),
  "graph.concat_cols": Object.freeze(["concat","pointwise"]),
  "graph.detach": Object.freeze(["pointwise"]),
  "graph.scale_const": Object.freeze(["pointwise"]),
  "graph.bias": Object.freeze(["pointwise"]),
  "graph.add": Object.freeze(["pointwise"]),
  "graph.mul": Object.freeze(["pointwise"]),
  "graph.conv1d": Object.freeze(["conv"]),
  "graph.conv2d": Object.freeze(["conv"]),
  "graph.relu2": Object.freeze(["pointwise"]),
  "graph.silu": Object.freeze(["pointwise"]),
  "graph.rmsnorm": Object.freeze(["pointwise"]),
  "graph.softmax": Object.freeze(["pointwise"]),
  "graph.causal_mask": Object.freeze(["pointwise"]),
  "graph.rope": Object.freeze(["rope"]),
  "graph.attention": Object.freeze(["attention"]),
  "loss.mse": Object.freeze(["pointwise"]),
  "loss.softmax_cross_entropy": Object.freeze(["softmax_xent"]),
  "loss.topk_knowledge_distillation": Object.freeze(["topk_kd"]),
  "optimizer.sgd": Object.freeze(["pointwise"]),
  "optimizer.adamw": Object.freeze(["adamw","adamw_terms","adamw_variance","adamw_finish"]),
  "optimizer.cautious_adamw": Object.freeze(["adamw","adamw_terms","adamw_variance","cautious_adamw_mask","cautious_adamw_lr","cautious_adamw_rescale","cautious_adamw_finish"]),
  "optimizer.int8_adamw": Object.freeze(["byte_codec","int8_adamw"]),
  "optimizer.muon": Object.freeze(["muon"]),
});

export const WEBGPU_DISPATCH_FORMS_V1 = Object.freeze({
  "graph.ste_surrogate|forward": Object.freeze({ operation: "graph.ste_surrogate", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":33,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.ste_surrogate|vjp": Object.freeze({ operation: "graph.ste_surrogate", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":34,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":1,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.salt_ste|forward": Object.freeze({ operation: "graph.salt_ste", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"salt","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "graph.salt_ste|vjp": Object.freeze({ operation: "graph.salt_ste", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":0,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.lsq_ste|forward": Object.freeze({ operation: "graph.lsq_ste", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":35,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.lsq_ste|vjp": Object.freeze({ operation: "graph.lsq_ste", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":36,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":37,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.fsq|forward": Object.freeze({ operation: "graph.fsq", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"fsq","entryPoint":"main","selector":null,"dispatch":"linear_input_64","repeat":"once"})]) }),
  "graph.fsq|vjp": Object.freeze({ operation: "graph.fsq", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"fsq","entryPoint":"main","selector":null,"dispatch":"linear_input_64","repeat":"once"})]) }),
  "graph.dense_matmul|forward": Object.freeze({ operation: "graph.dense_matmul", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":26,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.dense_matmul|vjp": Object.freeze({ operation: "graph.dense_matmul", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":27,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":28,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.ternary_matmul|forward": Object.freeze({ operation: "graph.ternary_matmul", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":29,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.ternary_matmul|vjp": Object.freeze({ operation: "graph.ternary_matmul", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":30,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":31,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":32,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.transpose|forward": Object.freeze({ operation: "graph.transpose", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":22,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.transpose|vjp": Object.freeze({ operation: "graph.transpose", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":23,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.embedding_gather|forward": Object.freeze({ operation: "graph.embedding_gather", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"embedding","entryPoint":"main","selector":null,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.embedding_gather|vjp": Object.freeze({ operation: "graph.embedding_gather", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"embedding","entryPoint":"main","selector":null,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.slice_cols|forward": Object.freeze({ operation: "graph.slice_cols", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":24,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.slice_cols|vjp": Object.freeze({ operation: "graph.slice_cols", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":25,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.concat_cols|forward": Object.freeze({ operation: "graph.concat_cols", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"concat","entryPoint":"main","selector":null,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.concat_cols|vjp": Object.freeze({ operation: "graph.concat_cols", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":24,"dispatch":"linear_output_64","repeat":"per_output"})]) }),
  "graph.detach|forward": Object.freeze({ operation: "graph.detach", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":0,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.detach|vjp": Object.freeze({ operation: "graph.detach", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":1,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.scale_const|forward": Object.freeze({ operation: "graph.scale_const", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":2,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.scale_const|vjp": Object.freeze({ operation: "graph.scale_const", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":2,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.bias|forward": Object.freeze({ operation: "graph.bias", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":18,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.bias|vjp": Object.freeze({ operation: "graph.bias", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":19,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":20,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.add|forward": Object.freeze({ operation: "graph.add", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":3,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.add|vjp": Object.freeze({ operation: "graph.add", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":0,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":0,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.mul|forward": Object.freeze({ operation: "graph.mul", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":4,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.mul|vjp": Object.freeze({ operation: "graph.mul", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":4,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":4,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.conv1d|forward": Object.freeze({ operation: "graph.conv1d", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"conv","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "graph.conv1d|vjp": Object.freeze({ operation: "graph.conv1d", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"conv","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "graph.conv2d|forward": Object.freeze({ operation: "graph.conv2d", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"conv","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "graph.conv2d|vjp": Object.freeze({ operation: "graph.conv2d", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"conv","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "graph.relu2|forward": Object.freeze({ operation: "graph.relu2", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":5,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.relu2|vjp": Object.freeze({ operation: "graph.relu2", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":6,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.silu|forward": Object.freeze({ operation: "graph.silu", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":7,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.silu|vjp": Object.freeze({ operation: "graph.silu", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":8,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.rmsnorm|forward": Object.freeze({ operation: "graph.rmsnorm", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":13,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.rmsnorm|vjp": Object.freeze({ operation: "graph.rmsnorm", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":14,"dispatch":"linear_output_64","repeat":"once"}),Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":15,"dispatch":"linear_primary_input_64","repeat":"once"})]) }),
  "graph.softmax|forward": Object.freeze({ operation: "graph.softmax", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":11,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.softmax|vjp": Object.freeze({ operation: "graph.softmax", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":12,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.causal_mask|forward": Object.freeze({ operation: "graph.causal_mask", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":9,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.causal_mask|vjp": Object.freeze({ operation: "graph.causal_mask", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":10,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "graph.rope|forward": Object.freeze({ operation: "graph.rope", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"rope","entryPoint":"main","selector":null,"dispatch":"rope_pairs_64","repeat":"once"})]) }),
  "graph.rope|vjp": Object.freeze({ operation: "graph.rope", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"rope","entryPoint":"main","selector":null,"dispatch":"rope_pairs_64","repeat":"once"})]) }),
  "graph.attention|forward": Object.freeze({ operation: "graph.attention", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"attention","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "graph.attention|vjp": Object.freeze({ operation: "graph.attention", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"attention","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "loss.mse|forward": Object.freeze({ operation: "loss.mse", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":16,"dispatch":"linear_primary_input_64","repeat":"once"})]) }),
  "loss.mse|vjp": Object.freeze({ operation: "loss.mse", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":17,"dispatch":"linear_output_64","repeat":"once"})]) }),
  "loss.softmax_cross_entropy|forward": Object.freeze({ operation: "loss.softmax_cross_entropy", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"softmax_xent","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "loss.softmax_cross_entropy|vjp": Object.freeze({ operation: "loss.softmax_cross_entropy", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"softmax_xent","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "loss.topk_knowledge_distillation|forward": Object.freeze({ operation: "loss.topk_knowledge_distillation", execution: "forward", stages: Object.freeze([Object.freeze({"moduleId":"topk_kd","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "loss.topk_knowledge_distillation|vjp": Object.freeze({ operation: "loss.topk_knowledge_distillation", execution: "vjp", stages: Object.freeze([Object.freeze({"moduleId":"topk_kd","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
  "optimizer.sgd|step": Object.freeze({ operation: "optimizer.sgd", execution: "step", stages: Object.freeze([Object.freeze({"moduleId":"pointwise","entryPoint":"main","selector":21,"dispatch":"linear_parameter_64","repeat":"once"})]) }),
  "optimizer.adamw|step": Object.freeze({ operation: "optimizer.adamw", execution: "step", stages: Object.freeze([Object.freeze({"moduleId":"adamw","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"adamw_terms","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"adamw_variance","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"adamw_finish","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"})]) }),
  "optimizer.cautious_adamw|step": Object.freeze({ operation: "optimizer.cautious_adamw", execution: "step", stages: Object.freeze([Object.freeze({"moduleId":"adamw","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"adamw_terms","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"adamw_variance","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"cautious_adamw_mask","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"cautious_adamw_lr","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"cautious_adamw_rescale","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"cautious_adamw_finish","entryPoint":"main","selector":null,"dispatch":"linear_parameter_64","repeat":"once"})]) }),
  "optimizer.int8_adamw|step": Object.freeze({ operation: "optimizer.int8_adamw", execution: "step", stages: Object.freeze([Object.freeze({"moduleId":"byte_codec","entryPoint":"unpack","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"byte_codec","entryPoint":"unpack","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"dequantize","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"square_variance","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"products","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"finish_products","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"finish_variance","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"update_parameter","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"reduce_scales","selector":null,"dispatch":"optimizer_blocks_256","repeat":"once"}),Object.freeze({"moduleId":"int8_adamw","entryPoint":"quantize","selector":null,"dispatch":"linear_parameter_64","repeat":"once"}),Object.freeze({"moduleId":"byte_codec","entryPoint":"pack","selector":null,"dispatch":"packed_words_64","repeat":"once"}),Object.freeze({"moduleId":"byte_codec","entryPoint":"pack","selector":null,"dispatch":"packed_words_64","repeat":"once"})]) }),
  "optimizer.muon|step": Object.freeze({ operation: "optimizer.muon", execution: "step", stages: Object.freeze([Object.freeze({"moduleId":"muon","entryPoint":"main","selector":null,"dispatch":"single","repeat":"once"})]) }),
});
