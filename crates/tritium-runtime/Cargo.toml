[package]
name = "tritium-runtime"
description = "Backend registry and dispatch — backends self-register via linkme; no concrete backend deps."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
tritium-spec = { workspace = true }
linkme = { workspace = true }

[dev-dependencies]
# Used by the doctest example and the in-crate dummy backend tests to spell out
# `GemmShape` / `TernaryFormat`. Not a runtime dependency: the registry itself
# only speaks the `tritium-spec` contract.
tritium-core = { workspace = true }

[lints]
workspace = true
