[package]
name = "tritium-quantize"
description = "SALT — sensitivity-allocated layered ternary quantization (residual planes + rate-distortion plane allocation)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true

# Offline (host-side) quantizer: turns fp weights into SALT ternary plane stacks
# and packs them through the TQ2_0 residual sidecar (tritium-format).
[dependencies]
tritium-core = { workspace = true }
tritium-format = { workspace = true }
half = { workspace = true }
blake3 = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
