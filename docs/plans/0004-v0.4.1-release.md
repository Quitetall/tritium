# 0004 — v0.4.1 release prep (CHANGELOG + version bump; STOP before tag)  (serves: v0.4.1)

## Goal

v0.4.1 is a **capability-neutral performance + correctness point-release** (like v0.3.1): it ships
the split-KV flash-decoding attention, the IMMA tail-shape OOB fix, the U5 fuzz targets, and the
v0.4.0 SALT runnable example — **no new public capability**, so the SALT capability staircase is
unchanged. This plan finalizes the release metadata: add the `## [0.4.1]` CHANGELOG section and bump
the workspace version `0.4.0 → 0.4.1`. **Success:** `cargo build --workspace` + `cargo test
--workspace` green, CHANGELOG + `Cargo.toml` consistent at `0.4.1`. **The executor STOPS before
tagging** — creating/pushing the `v0.4.1` tag is a release action the planner/user performs.

This plan is **blocked-by 0002 + 0003** (both must be committed first — they are: `a71c48e`,
`13438a4`). It is **doc/metadata only**; no source changes. Runs anywhere (CPU gates need no GPU;
the optional CUDA re-verify needs the GPU free).

## Preconditions
- Branch `main`, `git status` clean. `git log --oneline -1` shows `5ee4ce7` (gitignore .mimocode)
  or later.
- 0002 (`a71c48e`) and 0003 (`13438a4`) are in the history (`git log --oneline v0.4.0..HEAD` lists
  both). If either is missing, STOP — this plan must not run before them.

## Step 1 — Add the v0.4.1 CHANGELOG section

- **File:** `CHANGELOG.md`
- **Edit:** insert a new section **immediately above** the existing `## [0.4.0]` line (find the
  exact line `## [0.4.0] — 2026-06-19 — SALT quantization (ADR 0001/0006)` and insert the block
  *before* it, leaving the `[0.4.0]` section untouched):
  ```markdown
  ## [0.4.1] — 2026-06-20 — Split-KV decode attention + IMMA OOB fix (perf/correctness point-release)

  A capability-neutral point-release: it adds no new public API (the SALT staircase is unchanged),
  only performance and correctness depth on the v0.4.0 surface. Headline: flash-decoding (split-KV)
  attention wired into the resident M=N decode, and a fixed out-of-bounds read in the JIT IMMA
  kernel on tail shapes.

  ### Added
  - **Flash-decoding (split-KV) attention** (`tritium-cuda`): two new kernels —
    `gqa_attention_split_partial_f32` (warp per `(row, head, key-chunk)`; online-softmax over its
    key chunk → partial `{acc, m, l}`) and `gqa_attention_combine_f32` (flash-merge the `S` partials)
    — with a fixed chunk size so the captured CUDA-graph grid is valid for every decode step. Wired
    into **both** M=N launch sites (eager `decode_batch` + graph `decode_batch_graph`), so graph and
    eager stay bit-identical. An equivalence gate (`attn_split_kv_matches_direct_attention`) pins the
    split-KV output to the direct attention reference within tolerance.
  - **U5 fuzz targets** (`cargo-fuzz`) for the two v0.4.0 parsers (SALT bundle + SALT-in-GGUF).
  - **Runnable SALT example** (`crates/tritium-quantize/examples/salt_roundtrip.rs`): fp32 matrix →
    `quantize_tensor` → both containers → read back → identical-dequant assert (ADR 0002 U9).

  ### Fixed
  - **JIT IMMA tail-shape OOB read** (U7): the JIT `tq2_0_imma_mpgemm` kernel read 1–2 bytes past
    the packed weight buffer when an autotuned `tile_n > IMMA_N` and `N` was not a tile multiple (the
    high sub-tile index exceeded `ceil(N/IMMA_N)` n-tiles). Added an `nt` bound to the weight-stage
    read guard; the padding tile was already zeroed and masked on output, so results are unchanged.
    `compute-sanitizer memcheck` is now clean on the CUDA suite.
  - `cuda_batch_decode_matches_single` uses `sample_greedy` (deterministic tie-break, NaN-safe)
    instead of a panicking argmax.

  ### Performance
  - Split-KV cuts the decode attention kernel from **57.6% → 26.6%** of N=1 GPU time (~2.2× on the
    attention kernel; nsys per-kernel breakdown). End-to-end N=1 throughput is ~flat (N=1 is
    occupancy-bound across the whole pipeline — GEMM/rmsnorm/lm_head now dominate); no regression at
    N≥2 (clean re-bench: N=64 graph ≈ baseline). See `docs/plans/0001` outcome + `docs/ROADMAP.md`.

  ### Internal
  - `rustfmt` 1.9.0 across the workspace (greens the CI fmt gate); `.mimocode/` executor scratch
    gitignored.
  ```
- **Command:** `grep -n "## \[0.4.1\]\|## \[0.4.0\]" CHANGELOG.md | head`
- **Expected (PASS):** the `## [0.4.1]` line appears **above** the `## [0.4.0]` line (lower line
  number).
- **If `## [0.4.1]` is below `[0.4.0]` or absent:** you inserted in the wrong place — the new
  section must be the newest (top-most dated) entry. Move it directly above `## [0.4.0]`.
- **Paste:** the grep output.

## Step 2 — Bump the workspace version 0.4.0 → 0.4.1

- **File:** `Cargo.toml` (workspace root)
- **Edit:** find the exact line
  ```toml
  version = "0.4.0"               # SemVer (MAJOR.MINOR.PATCH); see CHANGELOG versioning note
  ```
  and change only the number:
  ```toml
  version = "0.4.1"               # SemVer (MAJOR.MINOR.PATCH); see CHANGELOG versioning note
  ```
  (All member crates use `version.workspace = true`, so this is the only version edit needed.)
- **Command:** `grep -rn "^version" Cargo.toml; grep -c "version.workspace = true" crates/*/Cargo.toml | grep -v ':0' | wc -l`
- **Expected (PASS):** `Cargo.toml` shows `version = "0.4.1"`; the second number is `11` (all 11
  member crates inherit the workspace version).
- **If a member crate pins its own `version = "0.4.0"`:** STOP and paste it — it would tag-mismatch;
  the planner decides whether to also bump it.
- **Paste:** the grep output.

## Gate 1 — workspace builds + CPU tests green at 0.4.1

```
cargo build --workspace 2>&1 | tail -3
cargo test --workspace 2>&1 | grep -E "test result:|error\[|FAILED" | tail -20
```
- **Expected (PASS):** `Finished`; every `test result:` line is `ok`; no `FAILED`. (This is the
  CPU/default-feature suite — the version bump must not break any crate's build.)
- **If a build error mentions a version/dependency mismatch:** an internal path-dep pinned `=0.4.0`
  — STOP and paste it.
- **If a test FAILS:** STOP and paste it (a release must not ship red).
- **Paste:** both outputs.

## Gate 2 — (CUDA, only if GPU free) full decode + IMMA re-verify

Run only if `nvidia-smi --query-gpu=memory.free --format=csv,noheader` shows ≥3 GB free. If a
`lamu`/`llama-server` holds VRAM, **STOP and report** (do not skip silently — a skip is not a pass).
```
cargo test -p tritium-nn --release --features cuda --test acceptance cuda_batch_decode -- --test-threads=1 2>&1 | grep -E "test .* \.\.\.|test result:|FAILED|skipping"
cargo test -p tritium-cuda --features cuda --lib imma 2>&1 | grep -E "test result:|FAILED|skipping"
```
- **Expected (PASS):** the three `cuda_batch_decode_*` tests `ok`; the `imma` tests `ok`; no
  `skipping`. (These already passed at 0003; this confirms the version bump changed nothing.)
- **If `skipping`/`0 tests`:** GPU not free → STOP and report (not a pass).
- **Paste:** both outputs.

## Commit  *(planner runs this after review — do not commit yourself)*

```
chore(release): v0.4.1 — split-KV decode attention + IMMA OOB fix

Capability-neutral perf/correctness point-release. CHANGELOG [0.4.1] + workspace
version bump 0.4.0 -> 0.4.1. Ships flash-decoding (split-KV) attention wired into
the resident M=N decode (attention 57.6% -> 26.6% of N=1 GPU time, no high-N
regression), the JIT IMMA tail-shape OOB fix (compute-sanitizer clean), U5 fuzz
targets for the v0.4.0 parsers, and a runnable SALT example. No new public API;
the SALT capability staircase is unchanged.

Co-Authored-By: MiMo 2.5 Pro <noreply@xiaomi.com>
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

## STOP — tagging is a release action  *(do NOT tag)*

After the commit lands, the release is **metadata-complete but UNtagged**. Creating `v0.4.1` and
pushing it is a deliberate release action the **planner/user** performs — the executor must **not**
run `git tag` or `git push --tags`. Report that the branch is ready to tag; the planner verifies the
full gate set (incl. the CUDA lane on a free GPU) and the user cuts the tag.

## Review  *(planner)*
Planner confirms the CHANGELOG accurately describes the `v0.4.0..HEAD` commit set, the version is
consistent across the workspace, and all gates are green (no skipped CUDA gate counted as a pass).

## Done criterion
`CHANGELOG.md` has a correct `## [0.4.1]` section above `[0.4.0]`; `Cargo.toml` is `0.4.1`; workspace
builds + CPU tests green; (if GPU available) CUDA decode + IMMA gates green; tree clean; **not
tagged** (handed to the planner/user). Report the diff + all gate outputs.
